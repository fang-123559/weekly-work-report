---
name: weekly-work-report
description: 每周工作周报（可视化）模板。读取一周的工作日志，生成美观直观的 HTML 可视化周报（工作概览/详细内容/资料汇总/趋势图表/下周计划）。用于每周工作周报自动化任务。触发词：每周工作周报、周报、工作周报、weekly report、周报可视化。
agent_created: true
---

# 每周工作周报（可视化）模板

每周自动生成**上周工作周报**，读取一周的工作日志文件，输出美观、直观的 HTML 可视化周报。

## 执行流程（必须严格遵守）

### 第一步：读取上周工作日志
1. 读取工作日志目录（如 `{PROJECT_DIR}/.workbuddy/memory/`）下上周（周一到周日）的所有 `.md` 文件
2. 提取每日工作内容，按日期整理

**失败模式编码**：
- 如果工作日志目录不存在 → 记录警告，创建空目录
- 如果某天没有日志文件 → 该日标注「暂无记录」
- 如果读取文件超时（>5秒） → 跳过该文件，记录警告

### 第二步：生成可视化周报（HTML）
创建一个美观、直观的 HTML 周报，包含：

- 📊 **工作概览**：上周工作天数、主要工作类型分布（用 CSS 图表）
- 📋 **详细工作内容**：按日期列出每日工作，区分不同类型（用标签）
- 🏆 **成果亮点**：本周关键成果（用高亮列表）
- 🔢 **活动统计**：各类工作活动的次数统计
- 📷 **工作资料汇总**：如果有照片或视频，添加缩略图或链接
- 📈 **工作趋势**：近五周记录天数统计（用柱状图）
- 💡 **本周计划**：基于上周进展，建议本周重点

**布局结构**（参考 `templates/weekly-report-template.html`）：
- **统计卡片**：工作日记录 / 主要类型占比 / 关键事项 / 无记录天数
- **周历热力图**：周一至周日每日状态（已记录/暂无/周末）+ 工作摘要
- **工作类型分布**：条形图（CSS 百分比宽度）
- **详细工作内容**：时间线（按日列出，含上午/下午）
- **本周成果亮点**：高亮列表（gold/green/purple 分类）
- **工作活动统计**：活动图标 + 名称 + 次数
- **工作趋势**：柱状图（近五周）
- **本周计划建议**：带优先级编号的建议列表

**CSS 规范**（从模板继承）：
- 主色：`#2563eb`（蓝），渐变头部 `linear-gradient(135deg, #1e3a8a, #3b82f6 55%, #60a5fa)`
- 背景：`#f0f4f8`，卡片白色 `#ffffff`，圆角 16px，柔和阴影
- 响应式：容器最大宽度 960px，居中

### 第三步：输出要求
- 保存为 `{PROJECT_DIR}/.workbuddy/memory/weekly-reports/YYYY-WXX.html`（如 2026-W33.html）
- 使用现代 CSS 设计：渐变背景、卡片布局、图标美化
- 包含可视化元素：用 HTML/CSS 实现简单的图表
- 响应式设计，适合在浏览器中查看
- 如果上周某天没有工作日志，在周报中标注「暂无记录」
- 保持专业、简洁的风格
- 重点突出工作成果和进展
- HTML 文件要独立完整，所有 CSS 内嵌

**失败模式编码**：
- 如果 HTML 生成失败 → 重试一次，仍失败则输出 Markdown 文本版周报
- 如果数据缺失（无任何日志） → 生成空模板周报，标注"本周无记录"

### 第四步：展示给用户
- 使用 `preview_url` 工具展示生成的 HTML 文件，让用户确认效果

## 反例与黑名单（不要做什么）

| # | 反模式 | 为什么不要做 | 替代做法 |
|---|--------|--------------|----------|
| 1 | **编造工作内容** | 周报必须基于真实日志 | 只使用工作日志中记录的内容 |
| 2 | **遗漏无记录日期** | 数据不完整，误导判断 | 无记录日期标注「暂无记录」 |
| 3 | **样式混乱/不可读** | 影响阅读体验 | 严格使用模板的 CSS 布局 |
| 4 | **无图表可视化** | 缺少直观展示 | 使用 CSS 柱状图/热力图 |
| 5 | **泄露敏感信息** | 周报含内部信息需注意 | 输出前检查是否有不当内容 |
| 6 | **忽略趋势分析** | 无法发现工作规律 | 必须包含近五周趋势统计 |
| 7 | **无下周计划** | 周报失去指导意义 | 必须基于上周进展给建议 |
| 8 | **HTML 不完整** | 无法独立打开 | 所有 CSS 内嵌，独立完整 |

**触发场景**：每轮执行前对照本表一次。任一反模式命中 → 立即修正。

---

## 异常处理（失败模式编码汇总）

| 场景 | 触发条件 | 处理动作 |
|------|----------|----------|
| 日志目录不存在 | memory 目录缺失 | 创建空目录，记录警告 |
| 某天无日志 | 当日无 .md 文件 | 标注「暂无记录」 |
| 读取超时 | 读取文件 >5秒 | 跳过该文件，记录警告 |
| HTML 生成失败 | 渲染超时/错误 | 重试一次，降级 Markdown |
| 全部无记录 | 一周无任何日志 | 生成空模板周报，标注本周无记录 |

**原则**：异常先记录警告，再按规则处理；绝不静默跳过或静默失败。

---

## 模板说明

`templates/weekly-report-template.html` 提供了完整的周报骨架，所有动态内容用 `{占位符}` 标注。执行时：
1. 复制模板为输出文件
2. 用真实数据替换所有占位符
3. 按需增删时间线条目（一天一个 `tl-item`）
